document.addEventListener('DOMContentLoaded', () => { const btn = document.querySelector('.menu-toggle'), nav = document.querySelector('.navbar nav'); if (btn && nav) { btn.addEventListener('click', () => nav.classList.toggle('open')) } document.querySelectorAll('form').forEach(f => f.addEventListener('submit', e => { e.preventDefault(); const m = f.querySelector('.form-message'); if (m) m.textContent = 'Formulario preparado. Falta conectar el envío a tu email/CRM.' })) });
